PART = ':\n                await _send_qh_flow_status_from_callback(query)\n                return\n\n            if action == \'dual_status\':\'\'\',\n    \'\'\'            if action == \'qh_status\':\n                await _send_qh_flow_status_from_callback(query)\n                return\n\n            if action == \'crowding_status\':\n                await _send_crowding_unwind_status_from_callback(query)\n                return\n\n            if action == \'dual_status\':\'\'\',\n    \'crowding status callback\',\n)\n\n# Triple labels and strategy allocator status in menu diagnostics.\ntext = emas.read_text(encoding=\'utf-8\')\ntext = text.replace(\'UTBreakout + RSPT-v2 + QH-Flow\', \'UTBreakout + RSPT-v3 + QH-Flow v2\')\ntext = text.replace(\'UTBreak + RSPT-v2 + QH-Flow\', \'UTBreak + RSPT-v3 + QH-Flow v2\')\nemas.write_text(text, encoding=\'utf-8\')\n\n# Integration assertions.\nintegration = ROOT / \'tests/test_qh_flow_integration.py\'\nappend_once(\n    integration,\n    \'test_crowding_and_allocator_are_registered\',\n    \'\'\'\ndef test_crowding_and_allocator_are_registered():\n    emas = _emas_module()\n    assert emas.CROWDING_UNWIND_STRATEGY in emas.UTBREAKOUT_STRATEGIES\n    assert emas.STRATEGY_DISPLAY_NAMES[emas.CROWDING_UNWIND_STRATEGY] == "FUNDING_OI_CROWDING_UNWIND"\n    assert {"crowd", "crowding", "crowding_status"} <= emas.UTBREAKOUT_CALLBACK_ACTIONS\n\n\ndef test_strategy_allocator_plan_hook_scales_once(monkeypatch):\n    emas = _emas_module()\n    engine = object.__new__(emas.SignalEngine)\n    engine.strategy_allocator_last_status = {}\n    engine._get_utbot_filtered_breakout_config = lambda *args, **kwargs: {\n        "strategy_allocator": {"minimum_samples": 1, "full_confidence_samples": 1}\n    }\n    engine._load_strategy_allocator_trades = lambda: [\n        {"strategy": emas.QH_FLOW_STRATEGY, "net_r": -1.0, "net_pnl": -1.0}\n    ]\n    plan = {"strategy": emas.QH_FLOW_STRATEGY, "qty": 2.0, "risk_usdt": 1.0}\n    first = engine._apply_strategy_allocator_to_plan(plan)\n    second = engine._apply_strategy_allocator_to_plan(first)\n    assert first["qty"] < 2.0\n    assert second["qty"] == first["qty"]\n\'\'\',\n)\n\n# README/current architecture note.\nreadme = ROOT / \'README.md\'\ntext = readme.read_text(encoding=\'utf-8\')\ntext = text.replace(\'**RSPT-v2**\', \'**RSPT-v3**\')\ntext = text.replace(\'RSPT-v2\', \'RSPT-v3\')\ntext = text.replace(\'QH-Flow\', \'QH-Flow v2\')\nif \'/utbreak crowding on|off|status\' not in text:\n    text = text.replace(\n        \'/utbreak qh on|off|status\\n\',\n        \'/utbreak qh on|off|status\\n/utbreak crowding on|off|status\\n\',\n        1,\n    )\nreadme.write_text(text, encoding=\'utf-8\')\n'
